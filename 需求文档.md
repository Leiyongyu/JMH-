# 跨境电商分销系统 — 开发需求文档

## 1. 文档目的

本文档用于明确跨境电商分销系统的完整建设范围，涵盖已实现的第一期基础功能与后续扩展阶段，供业务确认、产品设计、前后端开发、测试验收使用。

系统核心目标是：构建一个轻量级、中心化的 **"库存中台+分销系统"**，整合亚马逊 FBA 和谷仓的库存形成统一的"共享库存池"，作为链接分销商与领星 ERP 的桥梁，实现订单与发货数据的自动化流转。

---

## 2. 项目背景

当前跨境电商业务中，商品、库存等数据分散在领星 ERP（亚马逊 FBA 等平台库存）和谷仓 WMS（eBay 等平台库存）两个系统中。分销商需要一个独立系统查看可分销商品、确认共享库存与价格，并快速提交订单，同时系统需自动决策发货渠道（谷仓或亚马逊 FBA 多渠道配送），并将订单数据回传至领星 ERP。

### 2.1 现状（第一期已实现）

- 领星 ERP API 对接（OAuth + 签名 + HTTP 客户端）
- 库存明细从领星同步并展示
- eBay 商品从领星同步并展示
- 分销商下单（含商品快照与分销商快照）
- 管理员手动触发同步
- JWT 鉴权与角色权限

### 2.2 扩展目标

- 接入谷仓 WMS API，获取谷仓库存与创建出库订单
- 接入亚马逊 FBA 库存数据（优先通过领星 ERP 统一拉取）
- 建立跨系统 SKU 映射、分销商映射、利润/价格规则
- 构建共享库存池，聚合多来源库存
- 实现订单智能分流（谷仓发货 vs 亚马逊 FBA 多渠道配送）
- 订单回传领星 ERP，形成完整业务闭环
- 物流跟踪号回传

---

## 3. 建设目标

### 3.1 业务目标

1. 整合亚马逊 FBA 与谷仓库存，形成统一"共享库存池"，为分销商提供可视化可售库存。
2. 建立跨系统 SKU 映射，确保分销系统、领星 ERP、谷仓 WMS 三方数据一致。
3. 为每个分销商配置专属定价规则（成本价、基础售价、折扣、佣金）。
4. 分销商下单后，系统自动决策发货渠道并创建发货单。
5. 订单数据自动回传领星 ERP，与在领星 ERP 内操作 Walmart 多渠道配送逻辑一致。
6. 物流跟踪号自动回传，分销商可实时查看发货状态。

### 3.2 系统目标

1. 前端使用 React + TypeScript + Vite + Ant Design。
2. 后端使用 Node.js + NestJS + TypeScript。
3. 数据库使用 PostgreSQL（开发环境可用 sqljs）。
4. 外部系统集成逻辑与业务模块解耦，便于替换接口或扩展平台。
5. 领星 ERP 作为亚马逊 FBA 库存的统一入口（方案 A），避免直接对接亚马逊 SP-API 的高开发成本。

---

## 4. 用户角色

| 角色 | 说明 | 主要操作 |
|------|------|----------|
| 分销商 | 使用系统查看商品并下单的下游客户 | 登录、查看共享库存、查看商品、下单、查看自己的订单与物流状态 |
| 管理员 | 系统运营或内部人员 | 查看全量数据、触发同步、管理映射表、配置价格规则、查看订单、管理基础配置 |

---

## 5. 实施阶段总览

| 阶段 | 名称 | 核心内容 | 状态 |
|------|------|----------|------|
| 第一阶段 | 准备工作（基础设施） | API 对接打通、数据映射建立 | 领星已完成；谷仓待开发；映射表待开发 |
| 第二阶段 | 核心闭环（MVP） | 共享库存池、价格管理、订单分流、发货集成、订单回传 | 待开发 |
| 第三阶段 | 扩展优化 | 高级功能与监控（动销分析、告警、报表导出等） | 待规划 |

---

## 6. 第一阶段：准备工作（基础设施）

### 6.1 领星 ERP API 对接

**当前状态：已完成**

| 项目 | 说明 | 状态 |
|------|------|------|
| API 凭证 | `LINGXING_APP_ID`、`LINGXING_APP_SECRET` 配置于 `.env` | ✅ 已完成 |
| IP 白名单 | 需在领星开放平台添加服务器公网 IP | ⚠️ 需运维确认 |
| OAuth Token | `LingxingTokenService` 自动获取与缓存 `access_token` | ✅ 已完成 |
| 签名算法 | `LingxingSignService` 实现 MD5 + AES-ECB 签名 | ✅ 已完成 |
| HTTP 客户端 | `LingxingHttpService` 统一请求、重试、代理 | ✅ 已完成 |
| 库存明细同步 | `InventorySyncService` 定时/手动同步 | ✅ 已完成 |
| eBay 商品同步 | `ProductsSyncService` 定时/手动同步 | ✅ 已完成 |
| 基础数据同步 | SKU 主数据、店铺、仓库、供应商 | ⚠️ 部分完成（库存和商品已同步；店铺/仓库/供应商待扩展） |

**待扩展接口**：

| 接口 | 用途 | 优先级 |
|------|------|--------|
| 店铺列表 | 同步店铺信息，用于分销商映射 | 高 |
| 仓库列表 | 同步仓库信息，用于库存聚合维度 | 高 |
| 供应商列表 | 同步供应商信息，用于采购决策参考 | 中 |
| 亚马逊 FBA 库存 | 通过领星统一拉取 FBA 库存数据 | 高 |
| 订单创建/回传 | 将分销订单推送到领星 ERP | 高 |

### 6.2 谷仓（GoodCang）API 对接

**当前状态：未开发**

#### 6.2.1 凭证获取

- 登录谷仓开放平台 `https://oms.goodcang.com` 获取 `App Key` 和 `App Secret`
- 配置于 `.env`：`GOODCANG_APP_KEY`、`GOODCANG_APP_SECRET`

#### 6.2.2 核心接口开发

| 接口 | 用途 | 优先级 | 说明 |
|------|------|--------|------|
| `getSku` | 获取产品 SKU 列表 | 高 | 用于产品信息同步，建立 SKU 映射 |
| `getInventory` | 获取实时库存 | 高 | 用于共享库存池的谷仓库存数据源 |
| `createOrder` | 创建出库订单 | 高 | 分销商订单由谷仓发货时调用 |
| `getTrackingNumber` | 获取物流跟踪号 | 高 | 回传给分销商，更新订单物流状态 |

#### 6.2.3 技术实现要求

| 项目 | 要求 |
|------|------|
| 模块结构 | 新建 `GoodcangModule`，与 `LingxingModule` 平级，独立封装 HTTP 客户端、签名、错误处理 |
| 签名算法 | 按谷仓开放平台文档实现，需与官方调试工具验证一致 |
| 数据映射 | 新建 `goodcang/mappers/` 目录，集中维护字段转换 |
| 配置管理 | 所有凭证与路径通过 `.env` 注入，禁止硬编码 |
| 超时与重试 | 参照 `LingxingModule` 的重试与超时机制 |
| 日志脱敏 | 不输出 `App Secret`、完整 token |

### 6.3 亚马逊 FBA 库存数据同步

**当前状态：部分完成（领星库存明细接口已包含部分 FBA 数据）**

#### 方案选择

| 方案 | 说明 | 优缺点 | 建议 |
|------|------|--------|------|
| **方案 A（推荐）** | 利用领星 ERP 作为统一入口 | ✅ 开发成本低，复用现有 `LingxingModule`；❌ 依赖领星数据更新频率 | **采用** |
| 方案 B | 通过亚马逊 SP-API 自行对接 | ✅ 细颗粒度控制；❌ 需申请开发者账号，开发成本高 | 备选 |

#### 方案 A 实施要点

1. 确认领星库存明细接口返回的 FBA 仓库数据是否完整（`platform` 字段区分 FBA 与其他）。
2. 在 `inventory.mapper.ts` 中确保 FBA 库存的 `platform` 标识为 `AMAZON_FBA`。
3. 若领星提供独立的 FBA 库存接口，需新增同步路径配置（`.env` 中 `LINGXING_FBA_INVENTORY_PATH`）。

### 6.4 系统内数据映射与配置

**当前状态：未开发**

#### 6.4.1 SKU 映射表

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `distribution_sku` | 分销系统内的统一 SKU 编码 |
| `lingxing_sku` | 领星 ERP 中的物料编码/SKU |
| `goodcang_sku` | 谷仓 WMS 中的 SKU 编码 |
| `amazon_fnsku` | 亚马逊 FNSKU（可选） |
| `product_name` | 统一商品名称 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

**约束**：`distribution_sku` 唯一；`lingxing_sku` + `goodcang_sku` 组合唯一。

**维护方式**：初始批量导入（CSV/Excel），后续系统自动创建（同步时按规则匹配）。

#### 6.4.2 分销商映射表

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `distributor_user_id` | 分销系统中的用户 ID（FK → `users`） |
| `lingxing_shop_id` | 领星 ERP 中对应的"店铺"或"客户"对象 ID |
| `lingxing_shop_name` | 领星店铺名称 |
| `goodcang_customer_id` | 谷仓中的客户标识（可选） |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

**约束**：`distributor_user_id` 唯一。

**维护方式**：手动配置，明确每个分销商的"虚拟"店铺身份。

#### 6.4.3 利润/价格规则表

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `sku_mapping_id` | 关联 SKU 映射（FK → `sku_mappings`） |
| `distributor_user_id` | 分销商用户 ID（null 表示全局默认规则） |
| `cost_price` | 成本价（Decimal） |
| `base_price` | 基础售价（Decimal） |
| `discount_rate` | 折扣率（如 0.95 表示 95 折） |
| `commission_rate` | 佣金比例（如 0.05 表示 5%） |
| `currency` | 币种 |
| `min_order_qty` | 最小起订量 |
| `effective_from` | 生效开始时间 |
| `effective_to` | 生效结束时间（null 表示长期有效） |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

**维护方式**：手动配置，支持动态调整。优先匹配分销商+SKU 精确规则，其次匹配 SKU 全局默认规则。

---

## 7. 第二阶段：核心闭环开发（MVP）

### 7.1 模块 A：商品与库存中心（Inventory Hub）

#### 7.1.1 库存聚合

**功能说明**：通过定时任务，从领星 ERP 拉取聚合后的总库存（包括亚马逊 FBA 及其他本地仓），同时从谷仓拉取其仓库库存，汇总至分销平台"共享库存池"。

**数据源**：

| 数据源 | 接口 | 同步频率 | 说明 |
|--------|------|----------|------|
| 领星 ERP | 库存明细接口 | 每 5～15 分钟 + 手动触发 | 含 FBA + 本地仓 |
| 谷仓 WMS | `getInventory` | 每 5～15 分钟 + 手动触发 | 谷仓仓库库存 |

**共享库存池数据模型**：

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `sku_mapping_id` | 关联 SKU 映射（FK → `sku_mappings`） |
| `distribution_sku` | 分销系统统一 SKU |
| `source` | 库存来源：`LINGXING` / `GOODCANG` |
| `warehouse_code` | 仓库编码 |
| `warehouse_name` | 仓库名称 |
| `available_qty` | 可用库存 |
| `reserved_qty` | 预占库存 |
| `inbound_qty` | 在途库存 |
| `total_available_qty` | 跨来源汇总可用库存（按 SKU 聚合） |
| `synced_at` | 同步时间 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

**约束**：`(distribution_sku, source, warehouse_code)` 唯一，upsert 更新。

#### 7.1.2 库存更新与展示

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| INV-101 | 共享库存池同步 | 从领星 + 谷仓双源同步，汇总至统一库存池 |
| INV-102 | 按分销商可售库存展示 | 根据价格规则过滤：无价格规则的 SKU 对该分销商不可售 |
| INV-103 | 按 SKU/仓库查看 | 支持多维度筛选 |
| INV-104 | 库存看板升级 | 展示按来源（FBA/谷仓）分组的库存汇总 |
| INV-105 | 低库存预警 | 可配置阈值，低于阈值标红提示 |

**页面展示**：

| 展示项 | 说明 |
|--------|------|
| 共享库存总量 | 按 SKU 汇总的可用库存 |
| 按来源分组 | FBA 库存 / 谷仓库存 分别展示 |
| 最近同步时间 | 各来源最近一次成功同步时间 |
| 低库存 SKU 数 | 根据配置阈值统计 |
| 库存明细列表 | 支持按 SKU、来源、仓库搜索和分页 |

#### 7.1.3 动销与分析

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| INV-201 | 库存周转天数 | 基于聚合库存数据与订单出库量计算 |
| INV-202 | 动销率 | 统计周期内有出库记录的 SKU 占比 |
| INV-203 | 备货建议 | 基于动销率与库存周转天数，为备货决策提供数据支持 |

> 注：动销分析为第三阶段扩展功能，第二阶段预留数据结构与接口。

### 7.2 模块 B：价格与订单管理中心（Pricing & Order Hub）

#### 7.2.1 价格管理

**定价机制**：

| 机制 | 说明 |
|------|------|
| 手动定价 | 管理员在价格规则表中为特定分销商+SKU 配置价格 |
| 自动定价 | 系统根据成本价 + 加价率 + 折扣规则自动计算，公式：`最终售价 = 基础售价 × 折扣率` |

**价格规则匹配优先级**：

1. 精确匹配：`distributor_user_id` + `sku_mapping_id` 均匹配
2. SKU 默认：`distributor_user_id` 为 null，`sku_mapping_id` 匹配
3. 全局默认：`distributor_user_id` 为 null，`sku_mapping_id` 为 null

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| PRI-001 | 价格规则 CRUD | 管理员创建/编辑/删除价格规则 |
| PRI-002 | 批量导入规则 | 支持 CSV/Excel 批量导入价格规则 |
| PRI-003 | 分销商价格展示 | 分销商查看商品时展示其专属价格 |
| PRI-004 | 价格快照 | 下单时固化价格，后续规则变更不影响历史订单 |

#### 7.2.2 分销接口

**功能说明**：为分销商提供 API 接口和 Web 管理面板。

| 编号 | 功能 | 说明 |
|------|------|------|
| DIST-001 | 查询可售库存 | 分销商查询自己可售的 SKU 及库存 |
| DIST-002 | 查询专属价格 | 分销商查询 SKU 的专属售价 |
| DIST-003 | 提交订单 | 分销商提交下单请求 |
| DIST-004 | 取消订单 | 分销商在未发货前可取消订单 |
| DIST-005 | 查询订单状态 | 分销商查询订单状态与物流信息 |

#### 7.2.3 订单流程

**完整订单生命周期**：

```text
CREATED → PENDING → ROUTED → FULFILLING → SHIPPED → COMPLETED
                ↘ CANCELLED
```

| 状态 | 说明 |
|------|------|
| `CREATED` | 订单已创建（快照已保存） |
| `PENDING` | 订单进入待处理队列 |
| `ROUTED` | 系统已决策发货渠道 |
| `FULFILLING` | 已向发货方（谷仓/FBA）创建发货单 |
| `SHIPPED` | 已获取物流跟踪号 |
| `COMPLETED` | 订单完成 |
| `CANCELLED` | 订单已取消 |

**订单队列**：收到分销订单后，系统先将订单写入"待处理"队列，防止高峰时段堵塞。

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| ORD-101 | 订单创建 | 保存商品快照、分销商快照、价格快照 |
| ORD-102 | 订单入队 | 订单进入待处理队列 |
| ORD-103 | 订单取消 | 未发货前分销商可取消 |

#### 7.2.4 订单分流

**分流决策逻辑**：

系统根据订单的 SKU、收货地址、运费和发货时效，决策由谷仓发货还是由亚马逊 FBA 发货。

```text
订单进入 → 检查 SKU 库存来源
  ├─ SKU 仅在谷仓有库存 → 谷仓发货
  ├─ SKU 仅在 FBA 有库存 → FBA 多渠道配送
  ├─ SKU 两地均有库存 → 按规则决策：
  │    ├─ 收货地址为美国 → 优先 FBA
  │    ├─ 收货地址为非美国 → 优先谷仓
  │    └─ 运费/时效优先 → 按配置规则
  └─ SKU 无库存 → 标记缺货，通知管理员
```

**分流规则配置**：

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `priority` | 优先级（数值越小越优先） |
| `source` | 发货来源：`GOODCANG` / `AMAZON_FBA` |
| `destination_region` | 目的地区域（如 `US`、`EU`、`OTHER`） |
| `sku_pattern` | SKU 匹配模式（支持通配符） |
| `effective` | 是否启用 |

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| ORD-201 | 自动分流 | 系统根据规则自动决策发货渠道 |
| ORD-202 | 手动指定 | 管理员可手动指定发货渠道 |
| ORD-203 | 分流规则 CRUD | 管理员配置分流规则 |
| ORD-204 | FBA 多渠道配送 | 调用亚马逊 MCF API 或通过领星创建 FBA 出库单 |
| ORD-205 | 谷仓出库单 | 调用谷仓 `createOrder` 创建出库单 |

### 7.3 模块 C：订单回传与发货集成层（Integration Layer）

#### 7.3.1 订单转译与推送

**功能说明**：系统将分销订单转化为领星 ERP 能识别的格式，推送至领星"订单管理"模块。

**转译映射**：

| 分销系统字段 | 领星 ERP 字段 | 说明 |
|-------------|--------------|------|
| `order_no` | `order_id` / `reference_no` | 分销订单号作为领星外部单号 |
| `distributor_mapping.lingxing_shop_id` | `shop_id` | 分销商映射为领星店铺 |
| `order_line.sku` → `sku_mapping.lingxing_sku` | `sku` | SKU 转换为领星编码 |
| `order_line.qty` | `quantity` | 数量 |
| `order_line.unit_price_snapshot` | `price` | 价格 |
| `order.shipping_address` | `shipping_address` | 收货地址 |
| `order.remark` | `remark` | 备注 |

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| INT-001 | 订单转译 | 将分销订单转换为领星格式 |
| INT-002 | 推送领星 | 调用领星订单创建接口 |
| INT-003 | 推送状态回写 | 领星推送成功/失败后回写订单状态 |
| INT-004 | 重试机制 | 推送失败时自动重试，超过阈值告警 |

#### 7.3.2 谷仓发货集成

**流程**：

1. 订单分流决策为谷仓发货
2. 调用谷仓 `createOrder` 创建出库订单
3. 获取谷仓返回的出库单号，回写至分销订单
4. 定时轮询谷仓 `getTrackingNumber` 获取物流跟踪号
5. 物流跟踪号回写至分销订单，更新状态为 `SHIPPED`

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| GC-001 | 创建出库单 | 调用谷仓 `createOrder` |
| GC-002 | 出库单号回写 | 保存谷仓出库单号至订单 |
| GC-003 | 物流跟踪号获取 | 定时轮询 `getTrackingNumber` |
| GC-004 | 物流状态同步 | 同步谷仓发货状态至分销订单 |

#### 7.3.3 亚马逊 FBA 多渠道配送集成

**流程**：

1. 订单分流决策为 FBA 发货
2. 方案 A：通过领星 ERP API 创建 FBA 多渠道配送订单
3. 方案 B（备选）：通过亚马逊 SP-API 创建 MCF 订单
4. 获取配送单号与物流跟踪号
5. 回写至分销订单，更新状态为 `SHIPPED`

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| FBA-001 | 创建 FBA 出库单 | 通过领星 API 或 SP-API 创建 |
| FBA-002 | 配送单号回写 | 保存 FBA 配送单号至订单 |
| FBA-003 | 物流跟踪号获取 | 获取 FBA 物流跟踪号 |
| FBA-004 | 物流状态同步 | 同步 FBA 发货状态至分销订单 |

#### 7.3.4 物流跟踪号回传

**功能说明**：无论由谷仓还是 FBA 发货，物流跟踪号均需回传至分销商可见，并同步至领星 ERP。

**功能点**：

| 编号 | 功能 | 说明 |
|------|------|------|
| TRK-001 | 跟踪号展示 | 分销商在订单详情中查看物流跟踪号 |
| TRK-002 | 跟踪号回传领星 | 将物流跟踪号推送至领星 ERP |
| TRK-003 | 物流状态更新 | 定时同步物流状态（已发货/已签收等） |

---

## 8. 数据需求

### 8.1 完整数据表清单

| 表 | 用途 | 阶段 |
|----|------|------|
| `users` | 用户与角色 | 第一期 ✅ |
| `inventory_lines` | 领星库存明细 | 第一期 ✅ |
| `ebay_products` | eBay 商品 | 第一期 ✅ |
| `distributor_orders` | 订单主表 | 第一期 ✅ |
| `distributor_order_lines` | 订单明细表 | 第一期 ✅ |
| `sync_runs` | 同步任务记录 | 第一期 ✅ |
| `sku_mappings` | SKU 跨系统映射 | 第二期 |
| `distributor_mappings` | 分销商映射 | 第二期 |
| `price_rules` | 利润/价格规则 | 第二期 |
| `shared_inventory_pool` | 共享库存池 | 第二期 |
| `routing_rules` | 订单分流规则 | 第二期 |
| `fulfillment_records` | 发货记录（谷仓/FBA） | 第二期 |
| `tracking_records` | 物流跟踪记录 | 第二期 |
| `goodcang_products` | 谷仓产品数据 | 第二期 |

### 8.2 数据保存原则

1. 领星是亚马逊 FBA 库存的事实来源；谷仓是谷仓仓库库存的事实来源。
2. 本系统通过同步复制必要字段到本地数据库。
3. 订单必须保存快照（商品信息、价格、分销商信息），避免后续变化影响历史订单。
4. 外部接口原始响应可保存到 `raw_payload`（jsonb），便于排查字段变化。
5. SKU 映射是跨系统数据流转的核心纽带，必须保证映射关系的准确性与完整性。

---

## 9. 权限需求

| 功能 | 分销商 | 管理员 |
|------|--------|--------|
| 登录 | 可以 | 可以 |
| 查看共享库存 | 可以（仅可售 SKU） | 可以（全量） |
| 查看商品 | 可以 | 可以 |
| 查看专属价格 | 可以 | 可以 |
| 商品下单 | 可以 | 可以 |
| 取消订单（未发货） | 可以（自己的） | 可以（全部） |
| 查看自己的订单 | 可以 | 可以 |
| 查看全部订单 | 不可以 | 可以 |
| 查看物流跟踪 | 可以（自己的订单） | 可以（全部） |
| 手动同步 | 不可以 | 可以 |
| 管理 SKU 映射 | 不可以 | 可以 |
| 管理分销商映射 | 不可以 | 可以 |
| 管理价格规则 | 不可以 | 可以 |
| 管理分流规则 | 不可以 | 可以 |
| 手动指定发货渠道 | 不可以 | 可以 |

---

## 10. 接口需求概要

### 10.1 第一期已有接口

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/auth/login` | 登录 |
| `GET` | `/api/v1/me` | 获取当前用户 |
| `GET` | `/api/v1/inventory/summary` | 库存看板 |
| `GET` | `/api/v1/inventory/lines` | 库存明细列表 |
| `GET` | `/api/v1/products/ebay` | eBay 商品列表 |
| `POST` | `/api/v1/orders` | 创建订单 |
| `GET` | `/api/v1/orders` | 我的订单 |
| `GET` | `/api/v1/orders/:orderNo` | 订单详情 |
| `POST` | `/api/v1/admin/sync/inventory` | 管理员手动同步库存 |
| `POST` | `/api/v1/admin/sync/ebay-products` | 管理员手动同步商品 |

### 10.2 第二期新增接口

#### SKU 映射

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/sku-mappings` | SKU 映射列表 |
| `POST` | `/api/v1/admin/sku-mappings` | 创建 SKU 映射 |
| `PUT` | `/api/v1/admin/sku-mappings/:id` | 更新 SKU 映射 |
| `DELETE` | `/api/v1/admin/sku-mappings/:id` | 删除 SKU 映射 |
| `POST` | `/api/v1/admin/sku-mappings/import` | 批量导入 SKU 映射 |

#### 分销商映射

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/distributor-mappings` | 分销商映射列表 |
| `POST` | `/api/v1/admin/distributor-mappings` | 创建分销商映射 |
| `PUT` | `/api/v1/admin/distributor-mappings/:id` | 更新分销商映射 |
| `DELETE` | `/api/v1/admin/distributor-mappings/:id` | 删除分销商映射 |

#### 价格规则

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/price-rules` | 价格规则列表 |
| `POST` | `/api/v1/admin/price-rules` | 创建价格规则 |
| `PUT` | `/api/v1/admin/price-rules/:id` | 更新价格规则 |
| `DELETE` | `/api/v1/admin/price-rules/:id` | 删除价格规则 |
| `POST` | `/api/v1/admin/price-rules/import` | 批量导入价格规则 |
| `GET` | `/api/v1/price-rules/my-prices` | 分销商查询自己的专属价格 |

#### 共享库存池

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/inventory/pool` | 共享库存池列表 |
| `GET` | `/api/v1/inventory/pool/summary` | 共享库存汇总 |
| `GET` | `/api/v1/inventory/pool/available` | 分销商可售库存 |

#### 分流规则

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/admin/routing-rules` | 分流规则列表 |
| `POST` | `/api/v1/admin/routing-rules` | 创建分流规则 |
| `PUT` | `/api/v1/admin/routing-rules/:id` | 更新分流规则 |
| `DELETE` | `/api/v1/admin/routing-rules/:id` | 删除分流规则 |

#### 订单扩展

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/orders/:orderNo/cancel` | 取消订单 |
| `POST` | `/api/v1/admin/orders/:orderNo/route` | 手动指定发货渠道 |
| `GET` | `/api/v1/orders/:orderNo/tracking` | 查询物流跟踪 |

#### 谷仓同步

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/admin/sync/goodcang-inventory` | 手动同步谷仓库存 |
| `POST` | `/api/v1/admin/sync/goodcang-products` | 手动同步谷仓产品 |

---

## 11. 页面需求

### 11.1 第一期已有页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | `/login` | 邮箱+密码登录 |
| 库存看板页 | `/inventory` | 库存指标+明细表格 |
| eBay 商品页 | `/products` | 商品列表+下单 |
| 购物车页 | `/cart` | 购物车 |
| 我的订单页 | `/orders` | 订单列表 |
| 订单详情页 | `/orders/:orderNo` | 订单详情 |

### 11.2 第二期新增/升级页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 共享库存池 | `/inventory/pool` | 多来源库存聚合展示 |
| SKU 映射管理 | `/admin/sku-mappings` | SKU 映射 CRUD + 批量导入 |
| 分销商映射管理 | `/admin/distributor-mappings` | 分销商映射 CRUD |
| 价格规则管理 | `/admin/price-rules` | 价格规则 CRUD + 批量导入 |
| 分流规则管理 | `/admin/routing-rules` | 分流规则 CRUD |
| 订单管理升级 | `/orders` | 增加物流跟踪、发货渠道、状态流转 |
| 谷仓库存页 | `/inventory/goodcang` | 谷仓库存明细 |

---

## 12. 非功能需求

### 12.1 性能

1. 库存和商品列表必须分页。
2. SKU、同步时间、订单号等字段需要建立索引。
3. 前端列表支持搜索，不一次性加载全部数据。
4. 共享库存池查询需支持按 SKU、来源、仓库多维度筛选。
5. 订单分流决策响应时间 < 500ms。

### 12.2 安全

1. 所有业务接口都需要登录鉴权。
2. 管理员接口必须校验角色。
3. 领星 `APP_ID`、`APP_SECRET`、谷仓 `App Key`、`App Secret` 只能放在环境变量中。
4. 日志中不得输出完整 token 或 secret。
5. 分销商只能访问自己的订单与价格数据。

### 12.3 可维护性

1. 领星接口调用放在独立 `LingxingModule`。
2. 谷仓接口调用放在独立 `GoodcangModule`。
3. 外部系统返回字段到本地字段的转换应集中维护在 `mappers/` 目录。
4. 前后端 API 通过 Swagger 保持一致。
5. 订单分流逻辑可配置，不硬编码。

### 12.4 可观测性

1. 每次同步需要记录开始时间、结束时间、状态、成功条数、失败原因。
2. 同步失败不应影响用户查看已有商品、库存和订单。
3. 订单分流决策需记录日志（选择了哪个渠道、原因）。
4. 订单回传领星需记录推送结果（成功/失败/重试次数）。
5. 连续同步失败或回传失败需告警。

### 12.5 可靠性

1. 订单创建与发货单创建应在同一事务中（或使用 Saga 补偿机制）。
2. 谷仓/FBA 发货单创建失败时，订单回退至 `PENDING` 状态，可重试。
3. 物流跟踪号轮询需有退避策略，避免频繁调用外部 API。

---

## 13. 技术架构

### 13.1 系统架构图

```text
┌──────────────────────────────────────────────────────────────────┐
│  Web（Vite + React + Ant Design）                                 │
│  库存看板 | 共享库存池 | 商品列表 | 订单管理 | 管理后台             │
└────────────────────────────┬─────────────────────────────────────┘
                             │ HTTPS / JSON
┌────────────────────────────▼─────────────────────────────────────┐
│  NestJS API                                                       │
│  Auth | Inventory | Product | Order | Pricing | Routing | Sync    │
│  LingxingModule | GoodcangModule                                   │
└──────┬─────────────────────────────────┬─────────────────────────┘
       │                                 │
┌──────▼──────┐              ┌───────────▼───────────┐
│ PostgreSQL   │              │ 外部系统               │
│ 主库         │              │ ├─ 领星 ERP Open API   │
│              │◄──同步任务──►│ │  (FBA库存/订单回传)   │
└──────────────┘              │ ├─ 谷仓 WMS API        │
                              │ │  (库存/出库/跟踪号)   │
                              │ └─ 亚马逊 SP-API(备选) │
                              └───────────────────────┘
```

### 13.2 NestJS 模块划分

| 模块 | 职责 | 阶段 |
|------|------|------|
| `AuthModule` | JWT 登录、角色、密码哈希 | 第一期 ✅ |
| `UserModule` | 分销商用户资料 | 第一期 ✅ |
| `LingxingModule` | HTTP 客户端、签名/Token、统一错误与重试 | 第一期 ✅ |
| `GoodcangModule` | 谷仓 HTTP 客户端、签名、错误与重试 | 第二期 |
| `InventoryModule` | 同步任务、库存明细表、聚合查询、库存看板 | 第一期 ✅ |
| `ProductModule` | eBay 商品同步、列表 API | 第一期 ✅ |
| `OrderModule` | 创建订单（快照）、订单列表/详情、状态流转 | 第一期 ✅，第二期扩展 |
| `PricingModule` | 价格规则 CRUD、分销商价格查询 | 第二期 |
| `RoutingModule` | 订单分流决策、分流规则管理 | 第二期 |
| `FulfillmentModule` | 发货集成（谷仓出库/FBA MCF）、物流跟踪 | 第二期 |
| `MappingModule` | SKU 映射、分销商映射管理 | 第二期 |
| `SyncModule` | 同步任务调度与记录 | 第一期 ✅ |

---

## 14. 环境变量新增

### 14.1 谷仓相关

```env
# GoodCang WMS API
GOODCANG_BASE_URL=https://oms.goodcang.com
GOODCANG_APP_KEY=
GOODCANG_APP_SECRET=
GOODCANG_HTTP_TIMEOUT_MS=180000
GOODCANG_TRANSPORT_RETRIES=2
GOODCANG_TRANSPORT_RETRY_DELAY_MS=800
```

### 14.2 分流与发货相关

```env
# Order routing defaults
ROUTING_DEFAULT_SOURCE=GOODCANG
ROUTING_FBA_ENABLED=false
```

---

## 15. 里程碑

| 阶段 | 里程碑 | 内容 | 依赖 |
|------|--------|------|------|
| 第一阶段 | M1 | 领星 API 对接完成 | ✅ 已完成 |
| 第一阶段 | M2 | 谷仓 API 对接完成 | 谷仓凭证 |
| 第一阶段 | M3 | SKU 映射表 + 分销商映射表建立 | M1 + M2 |
| 第二阶段 | M4 | 共享库存池上线 | M3 |
| 第二阶段 | M5 | 价格规则管理上线 | M3 |
| 第二阶段 | M6 | 订单分流 + 谷仓发货集成 | M4 + M5 |
| 第二阶段 | M7 | FBA 多渠道配送集成 | M6 |
| 第二阶段 | M8 | 订单回传领星 + 物流跟踪号回传 | M6 + M7 |
| 第二阶段 | M9 | 全流程联调与上线 | M8 |

---

## 16. 验收标准汇总

### 16.1 第一期验收标准（已完成）

1. 用户可以登录并访问系统。
2. 分销商能查看库存数据。
3. 分销商能查看 eBay 商品数据。
4. 商品列表展示 SKU、库存、价格、物品链接和下单按钮。
5. 分销商能成功创建订单。
6. 订单中保存商品快照和分销商快照。
7. 分销商只能查看自己的订单。
8. 管理员可以触发库存和商品同步。
9. 领星 API 配置缺失或调用失败时，系统有明确错误提示。
10. 库存和商品列表支持搜索与分页。

### 16.2 第二期验收标准

1. 谷仓库存同步成功，数据可入库并展示。
2. 共享库存池正确聚合领星 + 谷仓双源库存。
3. SKU 映射表建立后，分销系统 SKU 可正确转换为领星/谷仓 SKU。
4. 分销商映射表建立后，订单可正确关联领星店铺。
5. 价格规则生效后，分销商看到的是专属价格。
6. 订单分流决策正确：根据规则选择谷仓或 FBA 发货。
7. 谷仓出库单创建成功，出库单号回写至分销订单。
8. FBA 多渠道配送单创建成功（若启用）。
9. 物流跟踪号获取成功，分销商可查看。
10. 订单回传领星 ERP 成功，领星中可查看对应订单。
11. 订单全流程状态流转正确：CREATED → PENDING → ROUTED → FULFILLING → SHIPPED → COMPLETED。
12. 发货失败时订单可回退至 PENDING 并支持重试。

---

## 17. 待确认问题

1. 谷仓 API 的具体文档版本与字段样例（需提供 JSON response 样例）。
2. 领星 ERP 是否提供"订单创建"接口？接口路径与字段格式？
3. 领星 ERP 是否提供"FBA 多渠道配送"接口？还是需要直接对接亚马逊 SP-API？
4. 库存看板最终展示哪些关键指标：按平台、仓库、店铺还是 SKU 维度？
5. 订单分流规则的默认策略：优先谷仓还是优先 FBA？
6. 分销商下单是否需要收货地址？如果需要，地址格式如何定义？
7. 是否需要管理员后台独立页面，还是集成在现有系统中？
8. 订单回传领星是创建"销售单"还是"草稿单"？
9. 物流跟踪号是否需要推送至分销商的外部系统（如分销商自有 ERP）？
10. 是否需要导出库存、商品、订单报表？

---

## 18. 版本记录

| 版本 | 说明 |
|------|------|
| v1.0 | 根据 `开发文档.md` 整理第一版需求文档 |
| v2.0 | 扩展第二阶段需求：谷仓对接、共享库存池、价格管理、订单分流、发货集成、订单回传 |
