# 跨境电商分销系统 — 开发文档

## 文档信息

| 项目 | 说明 |
|------|------|
| 业务范围 | B2B2C 场景中 **分销商看图下单**；库存与 listing 数据来源 **领星 ERP**（Open API） |
| 核心模块 | **库存**、**商品（eBay Listing）**、**订单** |
| 前端 | React + TypeScript + **Vite** + **Ant Design** |
| 后端 | Node.js + **NestJS** + TypeScript |
| 数据库 | **PostgreSQL**（主库） |
| 外部系统 | **领星**：库存明细（多平台）+「eBay 商品」列表/详情 |

> **领星接口路径**：请以贵司在 **领星开放平台** 控制台中的实际文档为准（不同版本与企业开通能力可能不一致）。下文描述 **同步策略与落库字段**，具体 URL、签名与分页参数在实施阶段对照官方文档填充。

---

## 1. 目标与边界

### 1.1 要解决什么

- 分销商在自有系统内看到 **可被分销的 SKU / eBay 商品**（含库存、售价、跳转链接）。
- **库存**：从领星拉取「多平台库存明细」入账，系统在库存模块只展示少量 **关键汇总/维度**。
- **商品**：从领星拉取「eBay 商品」入账，列表展示 **SKU、库存、价格、物品链接**，每行支持 **一键下单**。
- **订单**：分销商点击下单后，在库中固化 **下单时刻的商品信息与分销商身份信息**（快照），便于后续履约与对账。

### 1.2 明确不做（MVP）

- 不实现完整支付网关、物流轨迹、多级佣金结算（除非你后续增补需求）。
- 不在 MVP 内「反写」领星创建销售单（若需要，可作为二期：下单后推送到领星生成草稿单）。

### 1.3 成功标准（建议）

- 定时/手动同步领星数据成功可观测（任务日志、失败告警）。
- 商品列表与库存看板与库内数据一致（允许分钟级延迟）。
- 下单接口幂等，订单详情可完整还原「当时看见的内容」。

---

## 2. 角色

| 角色 | 能力 |
|------|------|
| 分销商 | 登录、浏览商品、发起下单、查看自己的订单 |
| 管理员（可选 MVP） | 查看全量订单、触发同步、配置领星凭证与同步频率 |

若第一期无独立后台，可用 **环境变量 + 种子账号** 完成管理员能力，后续再接管理端 UI。

---

## 3. 系统架构

### 3.1 逻辑架构

```text
┌─────────────────────────────────────────────────────────┐
│  Web（Vite + React + Ant Design）                        │
│  库存看板 | eBay 商品列表（下单）| 我的订单                 │
└───────────────────────────┬─────────────────────────────┘
                            │ HTTPS / JSON
┌───────────────────────────▼─────────────────────────────┐
│  NestJS API                                               │
│  Auth | Inventory | Product(eBay) | Order | Sync(Jobs)   │
└───────────┬─────────────────────────────┬───────────────┘
            │                              │
     ┌──────▼──────┐                ┌──────▼──────┐
     │ PostgreSQL   │                │ 领星 Open API │
     │ 主库         │◄──同步任务────►│ 库存 / eBay   │
     └─────────────┘                └──────────────┘
```

### 3.2 NestJS 模块划分（建议）

| 模块 | 职责 |
|------|------|
| `AuthModule` | JWT 登录、角色、密码哈希（bcrypt） |
| `UserModule` | 分销商用户资料 |
| `LingxingModule` | HTTP 客户端、签名/Token、统一错误与重试（隔离第三方） |
| `InventoryModule` | 同步任务、库存明细表、聚合查询、库存看板 API |
| `ProductModule` | eBay 商品同步、列表 API、与库存的关联展示（按 SKU） |
| `OrderModule` | 创建订单（快照）、订单列表/详情 |
| `SyncModule` / `ScheduleModule` | `@nestjs/schedule` 定时拉取；或 BullMQ 队列（流量大时） |

### 3.3 前端工程（Vite）

- 路由：`react-router-dom`
- 请求：`axios` + 拦截器注入 Token
- 状态：首版可用 **React Query** 做列表缓存与轮询（可选）
- UI：Ant Design **Table / Card / Modal / Form / message**

---

## 4. 领星集成设计

### 4.1 配置与安全

- `LINGXING_BASE_URL`（默认可用 `https://openapi.lingxing.com`）、`LINGXING_APP_ID`、`LINGXING_APP_SECRET` 放在 **环境变量**，禁止提交仓库；OAuth 与签名的调用顺序与信封见 [附录 A](#附录-a领星-open-api-响应-json-样例依据样例py)（摘自仓库内 `样例.py`）。
- 生产环境使用 **只读或最小权限** 的 API 授权（若领星支持按接口授权）。
- 对接口响应做 **落盘日志脱敏**（不落 appSecret、完整 token）。

### 4.2 同步策略

| 数据 | 建议频率 | 说明 |
|------|-----------|------|
| 库存明细 | 每 5～15 分钟 + 手动触发 | 多平台量大时可分页拉全量后 **增量**（若领星提供 `update_time` 类字段则按时间窗增量） |
| eBay 商品 | 每 15～60 分钟 + 手动触发 | 以 SKU 为主键做 upsert |

**失败处理**：记录 `sync_run`（开始/结束/条数/错误码/堆栈摘要）；连续失败告警（邮件/企业微信等二期）。

**幂等**：以领星侧稳定主键（如 `sku` + `platform` + `warehouse_id` 等，以实际返回为准）组合成系统内 `natural_key`，`ON CONFLICT` upsert。

### 4.3 与官网文档对齐（实施 checklist）

在实施阶段为每个接口建立 **Adapter 映射表**：领星字段 → 本地表字段（含类型与默认值）。

---

## 5. 数据模型（PostgreSQL）

以下用逻辑名描述；迁移时请加 `uuid`/`bigserial`、`created_at`、`updated_at`，并对查询字段建索引。

### 5.1 用户：`users`

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `email` | 登录邮箱（unique） |
| `password_hash` | bcrypt |
| `role` | `DISTRIBUTOR` / `ADMIN` |
| `display_name` | 展示名 |

### 5.2 库存明细：`inventory_snapshots` 或 `inventory_lines`

**用途**：承接领星「库存明细」原始结构的主子集（按实际 API 裁剪）。

建议至少保留（名称可按领星字段映射）：

- `sku`（索引）
- `platform` / `sales_channel`（多平台维度）
- `warehouse_code` / `warehouse_name`
- `available_qty`、`reserved_qty`、`inbound_qty` 等 **API Provided**
- `raw_payload` **jsonb（可选）**：完整一条响应，便于领星改版时快速对齐
- `synced_at`：**写入时间**，用于「数据新鲜度」展示

**约束**：`(sku, platform, warehouse_code)` 等业务唯一组合需与运营确认。

### 5.3 库存聚合（展示用）：`inventory_summaries`（可选）

若看板只做汇总，可增加物化视图或定时汇总表：

- `sku`
- `total_available`（跨仓/跨平台可加）
- `last_synced_at`

### 5.4 eBay 商品：`ebay_products`

MVP 展示字段（与前端一致）：

| 字段 | 说明 |
|------|------|
| `sku` | unique |
| `title` | 可选，便于运营辨认 |
| `stock_qty` | 同步自领星；可与库存模块 **按 SKU 对齐** |
| `price` | Decimal（货币与币种字段若 API 提供则一并存 `currency`） |
| `item_url` | eBay listing 跳转链接 |
| `lingxing_product_id` | 若有，仅存映射 |
| `synced_at` | — |

前端 **库存展示**可以与 `inventory_*` join，也可以直接信任 eBay API 中与 listing 对齐的库存字段（以实现为准，避免两套数字长期不一致）。

### 5.5 订单：`distributor_orders` + `distributor_order_lines`

**下单时快照**（推荐拆行表，便于一单多 SKU 扩展）。

**`distributor_orders`**

| 字段 | 说明 |
|------|------|
| `id` | PK |
| `order_no` | 可读单号 unique |
| `buyer_user_id` | FK → `users`（分销商） |
| `snapshot_buyer_email` / `snapshot_buyer_name` | 下单瞬间写入 |
| `status` | `CREATED` / `CANCELLED`（按需扩展） |
| `remark` | 可选 |
| `created_at` | — |

**`distributor_order_lines`**

| 字段 | 说明 |
|------|------|
| `order_id` | FK |
| `sku` | 快照 |
| `title_snapshot` | 快照 |
| `qty` | 数量（MVP 默认 1 也可） |
| `unit_price_snapshot` | 快照（Decimal） |
| `currency_snapshot` | 快照 |
| `item_url_snapshot` | 快照 |
| `ebay_product_ref` | 可选，记录当时 `ebay_products.id` |

**幂等**：同一用户对同一 SKU 的快速重复点击可用 **前端防抖 + 后端 `client_request_id`**（可选 header）约束。

---

## 6. 功能说明（按模块）

### 6.1 库存模块

**数据来源**：领星库存明细 Open API → 入库 → API 提供给前端。

**页面关键数据（示例，可按运营精简）**

- 最近同步时间
- SKU 总数 / 低库存 SKU 数量（阈值可配置）
- 按 **平台** 或 **仓库** 的可用库存汇总 Top 列表
- 支持按 SKU 搜索、分页

不涉及在本系统 **改库存**；库存事实源为领星。

### 6.2 商品模块（eBay）

**数据来源**：领星「eBay 商品」接口 → `ebay_products` upsert。

**列表列**

| 列 | 说明 |
|----|------|
| SKU | 主展示 |
| 库存 | 来自同步字段 |
| 价格 | Decimal + 货币 |
| 物品链接 | `Link` 新窗口打开 |
| 操作 | **下单** |

**下单交互**

1. 用户点击下单 → Modal 确认（数量 MVP 固定为 1 或可编辑）。
2. 调用 `POST /api/v1/orders`，服务端 **读取当前 `ebay_products` 行** 写入订单快照。
3. 成功 toast + 跳转「我的订单」或留在列表。

### 6.3 订单模块

- **分销商**：只看自己的订单；详情展示快照字段（不受后续领星改价影响）。
- **管理员（可选）**：全量筛选（时间、SKU、分销商邮箱）。

---

## 7. API 概要（自用 REST）

前缀：`/api/v1`。

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/login` | 返回 JWT |
| GET | `/me` | 当前用户 |
| GET | `/inventory/summary` | 看板汇总 |
| GET | `/inventory/lines` | 明细分页（可选） |
| GET | `/products/ebay` | eBay 商品分页/搜索 |
| POST | `/orders` | body: `sku`, `qty` |
| GET | `/orders` | 当前用户订单 |
| GET | `/orders/:orderNo` | 详情 |
| POST | `/admin/sync/inventory` | 管理员手动触发 |
| POST | `/admin/sync/ebay-products` | 管理员手动触发 |

鉴权：`Authorization: Bearer <token>`；管理员路由加角色守卫。

Swagger：使用 `@nestjs/swagger` 生成 OpenAPI，前后端对齐 DTO。

---

## 8. 非功能需求

| 类别 | 要求 |
|------|------|
| 可用性 | 同步任务失败不影响登录与浏览历史订单 |
| 性能 | 列表分页；大表对 `sku`、`synced_at` 索引 |
| 安全 | JWT 过期刷新策略（MVP 可短时 token）；HTTPS；CORS 白名单 |
| 审计 | 同步任务批次记录；管理员操作日志（二期） |

---

## 9. 目录与交付物（建议）

```text
apps/
  web/          # Vite React
  api/          # NestJS
packages/
  types/        # 共享 TS 类型（可选）
infra/
  docker-compose.yaml  # Postgres + api + web（可选）
```

**交付**：可运行的 `docker-compose`、`.env.example`、数据库 migrations（TypeORM/`prisma migrate`/Kysely 等任选其一）。

---

## 10. 里程碑

| 阶段 | 内容 |
|------|------|
| M1 | 用户登录、PostgreSQL、migrations、`LingxingModule` 空手调用通一条读接口 |
| M2 | 库存同步入库 + 库存看板页 |
| M3 | eBay 商品同步 + 列表 + 下单快照落库 |
| M4 | 订单列表/详情 polish、Swagger、基础部署文档 |

---

## 11. 待你确认的细节

1. 领星侧 **库存明细接口**与 **eBay 商品接口** 的具体文档版本与字段样例（贴一份 JSON response 最优）。
2. 「库存模块关键数据」运营最终要哪 3～5 个指标（可按平台、按仓库、按店铺维度？）。
3. 下单是否需要 **最小起订量、阶梯价**、备注必填、或多 SKU 购物车。
4. 是否需要独立 **管理后台** 页面，还是先仅后端 admin 路由 + Postman。
5. 币种：仅 USD 还是多币种展示。

---

## 附录 A：领星 Open API 响应 JSON 样例（依据样例.py）

> **`样例.py` 现状**：内含 Token 换取、签名生成与 **`GET …/erp/sc/data/account/lists`**（店铺/账号列表类接口）的请求方式；脚本在成功时把 `result["data"]` 数组里 **每一条记录** 写成一行 JSON（JSONL）。脚本 **没有写死业务 JSON 字面量**，下表信封与字段来自 **`样例.py` 中的判断逻辑**，**`data` 数组元素结构请以领星该接口开放平台文档或你本地抓包为准**。库存明细、eBay 商品等接口若在别的路径，**信封通常仍与同平台其它 Erp 读接口类似**，但 **`code` 类型可能仍要按接口区分做兼容**。

### A.1 获取 `access_token`（POST）

- **URL（样例）**：`https://openapi.lingxing.com/api/auth-server/oauth/access-token`
- **Body**：`application/x-www-form-urlencoded`，字段 `appId`、`appSecret`（见 `样例.py`）

**成功（脚本判断 `code === "200"`，注意为字符串）** — 结构示例：

```json
{
  "code": "200",
  "data": {
    "access_token": "<string>",
    "expires_in": 7200,
    "refresh_token": "<string>"
  }
}
```

**失败（脚本走 `else`）**：`code` 非 `"200"`，可能另含 `message` 等字段，以实际响应为准。

### A.2 业务读接口示例：`GET /erp/sc/data/account/lists`

- **Query**：`app_key`（= APP_ID）、`timestamp`（秒级字符串）、`access_token`、`sign`（见 A.3）
- **脚本成功条件**：顶层 `code === 0`（**数字**，与 Token 接口的 **字符串 `"200"` 不同**）
- **数据位置**：`data` 为 **数组**；脚本执行 `result_json_data.extend(result["data"])`

**成功 — 信封示例**（`data` 为数组；元素字段以领星该接口文档为准，或运行 `样例.py` 后查看写出的一行一条 JSONL）：

```json
{
  "code": 0,
  "data": []
}
```

**失败**：`code` 非 `0`，具体形态以开放平台为准。

### A.3 签名 `sign`（与 Node/NestJS 对齐时注意）

摘自 `Purchaser.generate_sign` / `format_params` / `aes_encrypt` 逻辑摘要：

1. **参与签名的参数**：至少包含发到 URL 的参数里除 `sign` 外的键值对（样例中为 `app_key`、`timestamp`、`access_token`）。
2. **canonical 字符串**：按键名 **升序** 排列；形如 `access_token=...&app_key=...&timestamp=...`。**值为 `""` 的键跳过**；若为 **对象或数组**，须用 **无空格的紧凑 JSON**，且对象键有序（Python 示例使用 `orjson` + `OPT_SORT_KEYS`），拼接为 `key=<json字符串>`。
3. 对 canonical 字符串做 **MD5**，取 **十六进制再转成大写**，得到一串 MD5 大写十六进制字符串。
4. 以 **`APP_ID`（`app_key`）为 AES 密钥**，对上述 MD5 结果做 **AES-ECB**，**PKCS7 补码**，结果 **Base64** 即为 `sign`。

NestJS 实现务必与开放平台调试工具或现有 Python 样例 **对同一参数算出相同 `sign`** 后再接通生产。

---

## 12. 版本记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | - | 按跨境电商 + 领星 + 库存/商品/订单 + 指定技术栈定稿 |
| v1.1 | - | 增加附录 A：基于 `样例.py` 的 Token / account/lists 响应信封与签名说明 |
