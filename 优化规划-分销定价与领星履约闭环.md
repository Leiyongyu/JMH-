# 分销系统优化规划（定价 + 领星履约闭环）

> 版本：v1.0  
> 日期：2026-05-09  
> 适用项目：`distribution-system`（NestJS + React）

---

## 1. 背景与目标

当前系统已具备：

- 领星库存与 eBay 商品同步
- 分销商下单
- 订单列表与详情

为支撑后续真实业务运营，需要优先完成你提出的两项优化：

1. **分销产品成本可手动维护（一口价）并自动计价给分销商**
2. **分销订单回传领星，支持自动发货或手动创建货件（海外仓多渠道）**

本文目标：给出可执行的分阶段设计，便于后续研发直接落地。

---

## 2. 优化点 A：成本维护 + 自动计价

## 2.1 业务目标

- 管理员可按 SKU 维护成本（一口价）
- 系统按规则自动计算分销售价
- 下单时固化“成本/售价/规则”快照，确保历史可追溯

## 2.2 核心规则（MVP）

- 成本来源优先级：`MANUAL > SYNC > DEFAULT`
- 售价公式（可配置）：
  - `salePrice = cost * (1 + markupRate) + fixedFee`
- 价格修整：
  - 默认保留 2 位小数

## 2.3 数据模型建议

### 表 1：`product_costs`

- `id`
- `sku`（unique）
- `manualCost`（decimal）
- `currency`
- `source`（MANUAL/SYNC/DEFAULT）
- `effectiveAt`（可选）
- `updatedBy`
- `updatedAt`

### 表 2：`pricing_rules`

- `id`
- `scope`（GLOBAL/DISTRIBUTOR）
- `distributorId`（scope=distributor 时使用）
- `markupRate`
- `fixedFee`
- `roundMode`
- `enabled`
- `updatedAt`

### 表 3：订单行快照增强（在 `order_lines`）

- `costSnapshot`
- `pricingRuleSnapshot`（json 或 ruleId）
- `marginSnapshot`（可选）

## 2.4 API 规划（管理端）

- `GET /admin/product-costs`
- `PUT /admin/product-costs/:sku`
- `POST /admin/product-costs/batch`
- `GET /admin/pricing-rules`
- `PUT /admin/pricing-rules/global`

## 2.5 服务层改造点

- 新增 `PricingService`
  - `resolveCost(sku)`
  - `resolveRule(user/distributor)`
  - `calcSalePrice(cost, rule)`

- 在 `OrdersService.create` 中：
  - 不直接使用商品同步价作为成交价
  - 调用 `PricingService` 计算并写入快照

## 2.6 前端改造点

### 管理员商品页

- 新增列：成本价（可编辑）、自动售价、价格来源
- 支持批量维护成本（后续）

### 分销商商品页

- 只展示分销价，不展示成本

---

## 3. 优化点 B：回传领星 + 自动/手动履约

## 3.1 业务目标

- 分销订单可回传领星
- 支持两种履约模式：
  - 自动发货（规则命中）
  - 手动创建货件（海外仓多渠道）
- 具备失败重试、人工补偿、审计追踪能力

## 3.2 关键原则

- 回传必须异步任务化（避免下单阻塞）
- 幂等优先（防重复建单/重复建货件）
- 状态机可观测（可追踪每次失败原因）

## 3.3 数据模型建议

### 表 1：`fulfillment_tasks`

- `id`
- `taskType`（PUSH_ORDER / CREATE_SHIPMENT / PULL_STATUS）
- `bizOrderId`
- `idempotencyKey`（unique）
- `status`（PENDING/RUNNING/SUCCESS/FAILED/DEAD）
- `attemptCount`
- `maxAttempts`
- `nextRetryAt`
- `lastError`
- `payload`（json）
- `result`（json）
- `createdAt/updatedAt`

### 表 2：`shipments`

- `id`
- `bizOrderId`
- `remoteShipmentNo`
- `warehouseCode`
- `carrier`
- `trackingNo`
- `status`
- `rawPayload`

### 表 3：`fulfillment_events`

- `id`
- `bizOrderId`
- `eventType`
- `message`
- `operator`
- `payload`
- `createdAt`

### 订单表扩展（`distributor_orders`）

- `pushStatus`
- `pushRetryCount`
- `pushLastError`
- `pushedAt`
- `remoteOrderNo`
- `fulfillmentMode`（AUTO/MANUAL）
- `routeDecision`

## 3.4 模块与文件规划（Nest）

- `fulfillment/`
  - `fulfillment.module.ts`
  - `fulfillment.service.ts`
  - `fulfillment.controller.ts`
  - `workers/fulfillment.worker.ts`
  - `entities/fulfillment-task.entity.ts`
  - `entities/shipment.entity.ts`
  - `entities/fulfillment-event.entity.ts`

- `mapping/`（建议同步建设）
  - `sku-mapping.entity.ts`
  - `warehouse-mapping.entity.ts`
  - `store-mapping.entity.ts`

## 3.5 API 规划（管理员）

- `POST /admin/fulfillment/orders/:orderNo/push`
- `POST /admin/fulfillment/orders/:orderNo/shipments`
- `GET /admin/fulfillment/tasks`
- `POST /admin/fulfillment/tasks/:id/retry`
- `GET /admin/fulfillment/orders/:orderNo/events`

## 3.6 Worker 策略

- 扫描 `PENDING/FAILED` 且 `nextRetryAt <= now` 的任务
- 失败按指数退避重试
- 超过最大重试进入 `DEAD`
- 提供人工重试入口

---

## 4. 跨系统映射与路由规则（必须）

至少要维护三类映射：

1. 本地 SKU ↔ 领星 SKU
2. 本地仓库 ↔ 领星仓库
3. 分销渠道 ↔ 领星店铺/平台账号

路由规则至少包含：

- 优先仓
- 可拆单策略
- 自动/手动触发阈值
- 异常地区/高风险订单转人工

---

## 5. 分阶段落地计划（建议）

## Phase 1（优先，1~2 周）

- 完成 `fulfillment_tasks` + worker + 订单回传任务化
- 管理端任务列表与手动重试
- 订单表回传状态字段打通

## Phase 2（1 周）

- 成本维护与全局计价规则
- 商品页显示自动售价
- 下单快照接入成本/计价规则

## Phase 3（1~2 周）

- 手动创建货件流程
- 自动发货路由规则
- 异常补偿台与履约事件审计

---

## 6. 风险与注意事项

- **重复下发风险**：必须使用 `idempotencyKey`
- **字段不齐导致回传失败**：先做字段完整性校验
- **历史订单价格漂移**：必须写快照，不能实时重算
- **运营不可见失败**：必须有失败列表 + 可重试
- **权限风险**：成本、发货、重试仅管理员

---

## 7. 验收标准（MVP）

满足以下即可认为第一阶段可上线：

1. 管理员可维护 SKU 成本并看到自动售价
2. 分销商下单使用自动售价，订单落成本/售价快照
3. 订单创建后生成回传任务，异步推送领星
4. 失败任务可查看错误并手动重试
5. 订单详情可看到回传状态与领星单号（如有）

---

## 8. 当前项目建议下一步（立即执行）

1. 先落地 `Phase 1` 任务化回传骨架（实体+模块+worker+admin接口）
2. 再做成本维护与计价（`Phase 2`）
3. 最后接自动/手动履约（`Phase 3`）

该顺序可最大化降低改造风险，并快速形成业务闭环。

